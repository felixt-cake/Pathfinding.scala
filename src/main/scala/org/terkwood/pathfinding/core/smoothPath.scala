package org.terkwood.pathfinding.core

import org.terkwood.pathfinding.core.models.{Path, Grid}

object smoothPath {
  /**
    * Smooth the give path.
    * The original path will not be modified; a new path will be returned.
    *
    * @param   grid The grid
    * @param   path The path
    */
  def apply(grid: Grid, path: Path) = ???
}
